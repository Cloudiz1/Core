#include <stdio.h>
#include "include/cstr.h"

int main(void) {
    // String *test_str = snew("test string");
    // sprintln(test_str);
    // sadd(test_str, "123456789");
    // sprintln(test_str);

    // String *str = snew("Hello, world!");
    // String *strslice = sslice(str, -6, -1);
    // sprintln(strslice);

    // String *str = snew("Hello, world!");
    // sprintln(supper(str));
    // sprintln(slower(str));
    // sprintln(str);

    // String *str = snew("Hello, world!");
    // sprintln(str);
    // sclear(str);
    // printf("%i", sisempty(str));

    // String *str = snew("");
    // sprintln(str);
    // printf("%i", sisempty(str));

    // String *str = snew("ddddddaaaaaaaabbbbb");
    // sreplace(str, "aaa", "abc");
    // sprintln(str);
}